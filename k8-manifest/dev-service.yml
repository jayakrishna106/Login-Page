#dev service file
apiVersion: v1
kind: Service
metadata: 
  name: login-svc
  namepsace: dev

spec:
  type: NodePort
  selector:
    app: login
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007